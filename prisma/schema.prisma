generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")  // Supabase 6543
  directUrl = env("DIRECT_URL")    // Supabase 5432 (für Migrationen)
}


// Benutzermodell
model User {
  // Schema hier einfügen aus meinem prisma-schema Artifact
}
