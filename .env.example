# Supabase – für die App (Serverless): Pooled Port 6543
# Beispiel (USER, PASS, HOST, DB ersetzen):
# postgresql://USER:PASS@HOST:6543/DB?pgbouncer=true&connection_limit=1&sslmode=require
DATABASE_URL=

# Supabase – für Migrationen: Direkter Port 5432
# Beispiel (USER, PASS, HOST, DB ersetzen):
# postgresql://USER:PASS@HOST:5432/DB?sslmode=require
DIRECT_URL=
